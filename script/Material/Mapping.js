/**
 * Texture will be in Polar cordinates 
 * Object is 3D Cordinates
 * define Transofmators from (u,v) space to (3-D)one
 * texture input is a buffer (array of [x,y,rgb])
*/

class Mapping {
    constructor(texture,he3DObject){
        
    }
 
}